# Znhaas BLE SDK 隐私协议介绍

## 1. 文档说明

本文档用于说明 `Znhaas BLE SDK` 在 Android 端的权限使用、信息处理范围和接入方责任，供产品、研发、测试和法务评审使用。

本文档为 SDK 隐私说明介绍稿，建议在对外发布或在正式隐私政策中引用前，由业务、法务和合规团队进一步确认。

## 2. SDK 概要

`Znhaas BLE SDK` 用于支持龙湖 `H07` 安全帽 BLE 通信，核心能力包括：

- 蓝牙打开引导
- 蓝牙扫描
- 蓝牙连接
- 设备通知监听
- 录像控制指令发送

SDK 当前仅面向蓝牙业务场景，不包含账号体系、支付、消息推送、地图、广告或埋点统计能力。

## 3. SDK 处理的信息范围

在使用过程中，SDK 可能处理以下信息：

### 3.1 蓝牙设备信息

- 蓝牙广播名称
- 蓝牙 MAC 地址
- RSSI 信号强度
- 设备连接状态
- GATT 服务与特征信息
- 设备通知回传数据

### 3.2 终端权限状态信息

- 蓝牙权限是否已授予
- 位置信息权限是否已授予
- 系统蓝牙开关状态

### 3.3 指令交互信息

- 发送给安全帽设备的控制指令
- SDK 生成的请求标识 `requestId`（用于业务侧调用关联）
- 指令发送时间戳
- 设备回传的协议数据

## 4. SDK 使用权限说明

SDK 涉及的 Android 权限如下：

### 4.1 蓝牙相关权限

- `android.permission.BLUETOOTH`
- `android.permission.BLUETOOTH_ADMIN`
- `android.permission.BLUETOOTH_SCAN`
- `android.permission.BLUETOOTH_CONNECT`

用途说明：

- 用于发现附近 BLE 设备
- 用于连接安全帽设备
- 用于读取设备广播信息和通信状态

### 4.2 位置相关权限

- `android.permission.ACCESS_COARSE_LOCATION`
- `android.permission.ACCESS_FINE_LOCATION`

用途说明：

- 在 Android 6 到 Android 11 及部分系统实现中，BLE 扫描依赖位置权限
- SDK 不以定位服务为业务目的，不主动进行地图定位、轨迹记录或地理围栏处理

## 5. SDK 信息使用目的

SDK 处理上述信息的目的仅限于实现以下功能：

1. 识别并筛选蓝牙名称前缀为 `znhaas` 的安全帽设备
2. 建立与目标安全帽的 BLE 通信连接
3. 监听设备上报的通知数据
4. 下发录像控制与物理按键控制指令
5. 回传蓝牙交互结果给接入应用进行业务展示

## 6. SDK 不涉及的信息类型

SDK 本身不主动采集、读取或上传以下信息：

- 通讯录
- 短信
- 通话记录
- 麦克风录音
- 相机画面
- 相册内容
- 日历
- 剪贴板
- 身份证号
- 银行卡信息
- 人脸信息
- 精确地理位置轨迹
- 用户账号密码

## 7. 数据传输与存储说明

### 7.1 SDK 本身的数据处理方式

- SDK 主要在本地内存中处理蓝牙扫描、连接和指令收发数据
- SDK 当前版本不内置业务服务端上报能力
- SDK 不主动将蓝牙设备信息上传至开发者服务器或第三方服务器

### 7.2 日志说明

- 若接入应用开启调试日志，部分蓝牙交互结果可能以日志形式输出到本地调试环境
- 该类日志默认用于研发调试，不建议在生产环境输出敏感业务信息

### 7.3 持久化说明

- SDK 默认不内置数据库持久化
- 是否保存设备信息、交互记录或回传内容，由接入应用自行决定并负责合规处理

## 8. 第三方组件说明

SDK 当前使用以下第三方蓝牙组件：

- `no.nordicsemi.android:ble`

其中 BLE 扫描能力基于 Android 原生 BLE API 实现，第三方组件主要用于 BLE 连接与通信管理。根据当前集成方式，SDK 侧未发现其主动进行业务数据上传的逻辑；如后续第三方版本升级，建议重新进行合规复核。

## 9. 接入方责任说明

接入方在使用本 SDK 时，应履行以下义务：

1. 在应用隐私政策中明确说明蓝牙权限、位置权限的使用目的
2. 在申请系统权限前，以合理方式向用户告知用途
3. 对通过 SDK 获取的设备信息、控制数据和回传数据承担合规责任
4. 若接入方自行将 SDK 数据上传服务器，应单独说明上传目的、保存期限和共享范围
5. 若接入方在日志、埋点、监控中记录了蓝牙数据，应自行完成脱敏和权限控制

## 10. 用户授权与控制

用户可以通过以下方式控制 SDK 功能：

- 拒绝蓝牙权限申请
- 拒绝位置权限申请
- 关闭系统蓝牙开关
- 在系统设置中撤回已授权权限

当用户拒绝相关权限或关闭系统蓝牙时，SDK 的扫描、连接和控制功能可能无法正常使用。

## 11. 适用范围与限制

本说明仅适用于当前版本 `Znhaas BLE SDK` 的 Android 端实现，不适用于：

- 接入方自行扩展的业务逻辑
- 接入方自行采集、上传或缓存的数据
- 非本 SDK 内置的其他第三方库

## 12. 版本信息

- SDK 名称：`Znhaas BLE SDK`
- 当前版本：`1.0.3`
- 适用平台：Android
- 最低系统版本：`API 22`

## 13. 建议对外披露口径

如需在应用隐私政策中引用，可参考以下简版描述：

> 为实现 H07 安全帽蓝牙扫描、连接、状态监听和控制功能，我们集成了 Znhaas BLE SDK。该 SDK 可能申请蓝牙权限和位置信息权限，用于发现并连接附近的安全帽设备，读取设备基础蓝牙信息并发送控制指令。SDK 本身不主动上传账号、通讯录、短信、相册、通话记录等个人信息。

## 14. 更新说明

当 SDK 的权限范围、数据处理逻辑、第三方依赖或对外能力发生变化时，应同步更新本说明文档，并重新进行隐私与合规评审。
